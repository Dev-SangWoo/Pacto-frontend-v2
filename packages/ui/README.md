# @pacto/ui

공통 UI 컴포넌트를 제공하는 패키지다.

이 패키지는 Pacto 도메인 지식을 갖지 않는다. 버튼, 입력, 모달, 배지, 테이블 같은 재사용 가능한 UI primitive와 조합 컴포넌트를 관리한다.

## 책임

```txt
공통 UI 컴포넌트
공통 레이아웃 primitive
접근성 기반 UI wrapper
디자인 토큰과 class helper
```

## 제외

```txt
캠페인 상태 해석
지갑 금액 계산
API 호출
권한 정책
```
