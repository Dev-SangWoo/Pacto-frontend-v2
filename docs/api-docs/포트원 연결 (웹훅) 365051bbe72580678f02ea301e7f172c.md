# 포트원 연결 (웹훅)

Dev Test: 시작 전
Local Test: 시작 전
Method: POST
URL: /api/v1/payments/webhook
담당: 지원 황
도메인: Payment

프론트에서 요청X

포트원에서 우리서버로 보내는 요청

```python
[포트원 결제 흐름]

광고주
 ↓
Frontend
 ↓
BE2
 ↓ POST /api/v1/payments/prepare
merchant_uid 생성
 ↓
Frontend
 ↓
PortOne 결제창 호출
 ↓
사용자 카드 결제
 ↓
PortOne 결제 완료
 ↓
(1) Frontend success callback
(2) PortOne → BE2 웹훅 호출
        **POST /api/v1/payments/webhook**
 ↓
BE2 서버
 ↓
PortOne API 재조회 검증
 ↓
BE1 Ledger API 호출
 ↓
광고주 예치금 생성
```

```python
카드 승인 완료
   ↓
 ┌────────────────────┐
 │                    │
 │ (A) success callback
 │      ↓
 │   Frontend UI 처리
 │
 │
 │ (B) webhook
 │      ↓
 │ POST /payments/webhook
 │      ↓
 │     [BE2]
 │      ↓
 │ PortOne API 재검증 (-> 포트원 서버에 실제 결제 완료 된 기록이 있는지 확인)
 │      ↓
 │ payment 저장
 │      ↓
 │ BE1 Ledger API 호출
 │      ↓
 │ 예치금 생성
 └────────────────────┘
```
